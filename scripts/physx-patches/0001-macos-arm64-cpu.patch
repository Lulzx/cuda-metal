diff --git a/physx/include/foundation/PxSIMDHelpers.h b/physx/include/foundation/PxSIMDHelpers.h
index 8d29cc2..51de386 100644
--- a/physx/include/foundation/PxSIMDHelpers.h
+++ b/physx/include/foundation/PxSIMDHelpers.h
@@ -48,7 +48,7 @@ namespace physx
 			const QuatV qV = V4LoadU(&q.x);
 			Vec3V column0V, column1V, column2V;
 			QuatGetMat33V(qV, column0V, column1V, column2V);
-#if defined(PX_SIMD_DISABLED) || (PX_LINUX && (PX_ARM || PX_A64))
+#if defined(PX_SIMD_DISABLED) || PX_ARM_FAMILY
 			V3StoreU(column0V, column0);
 			V3StoreU(column1V, column1);
 			V3StoreU(column2V, column2);
diff --git a/physx/snippets/compiler/cmake/linux/CMakeLists.txt b/physx/snippets/compiler/cmake/linux/CMakeLists.txt
index 7f6e32d..dbb372c 100644
--- a/physx/snippets/compiler/cmake/linux/CMakeLists.txt
+++ b/physx/snippets/compiler/cmake/linux/CMakeLists.txt
@@ -41,11 +41,16 @@ SET(CMAKE_CXX_FLAGS_PROFILE ${PHYSX_CXX_FLAGS_PROFILE})
 SET(CMAKE_CXX_FLAGS_RELEASE ${PHYSX_CXX_FLAGS_RELEASE})
 
 # Build PDBs for all configurations
-SET(CMAKE_SHARED_LINKER_FLAGS "/DEBUG")
-
-SET(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} -Wl,-rpath='$ORIGIN'")
+IF(NOT APPLE)
+	SET(CMAKE_SHARED_LINKER_FLAGS "/DEBUG")
+	SET(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} -Wl,-rpath='$ORIGIN'")
+ENDIF()
 
 # Include all of the projects
 SET(PLATFORM_SNIPPETS_LIST LoadCollection)
 
-SET(SNIPPET_RENDER_ENABLED 1)
+IF(APPLE)
+	SET(SNIPPET_RENDER_ENABLED 0)
+ELSE()
+	SET(SNIPPET_RENDER_ENABLED 1)
+ENDIF()
diff --git a/physx/snippets/compiler/cmake/linux/SnippetTemplate.cmake b/physx/snippets/compiler/cmake/linux/SnippetTemplate.cmake
index 5131fe1..202b7fb 100644
--- a/physx/snippets/compiler/cmake/linux/SnippetTemplate.cmake
+++ b/physx/snippets/compiler/cmake/linux/SnippetTemplate.cmake
@@ -28,10 +28,16 @@
 # Build Snippet linux template
 #
 
+IF(APPLE)
+	SET(SNIPPET_RENDER_DEF "")
+ELSE()
+	SET(SNIPPET_RENDER_DEF "RENDER_SNIPPET")
+ENDIF()
+
 SET(SNIPPET_COMPILE_DEFS
 	# Common to all configurations
 
-	${PHYSX_LINUX_COMPILE_DEFS};RENDER_SNIPPET
+	${PHYSX_LINUX_COMPILE_DEFS};${SNIPPET_RENDER_DEF}
 
 	$<$<CONFIG:debug>:${PHYSX_LINUX_DEBUG_COMPILE_DEFS};>
 	$<$<CONFIG:checked>:${PHYSX_LINUX_CHECKED_COMPILE_DEFS};>
@@ -46,14 +52,18 @@ SET(SNIPPET_PLATFORM_SOURCES
 SET(SNIPPET_PLATFORM_INCLUDES
 )
 
-# gwoolery: aarch64 requires glut library to be lower case, for whatever reason
-IF(CMAKE_SYSTEM_PROCESSOR STREQUAL "aarch64")
-	SET(GLUT_LIB "glut")
+IF(APPLE)
+	SET(SNIPPET_PLATFORM_LINKED_LIBS "")
 ELSE()
-	SET(GLUT_LIB "GLUT")
-ENDIF()
+	# gwoolery: aarch64 requires glut library to be lower case, for whatever reason
+	IF(CMAKE_SYSTEM_PROCESSOR STREQUAL "aarch64")
+		SET(GLUT_LIB "glut")
+	ELSE()
+		SET(GLUT_LIB "GLUT")
+	ENDIF()
 
-SET(SNIPPET_PLATFORM_LINKED_LIBS SnippetRender GL GLU ${GLUT_LIB} X11 rt pthread dl -Wl,-rpath='${ORIGIN}')
+	SET(SNIPPET_PLATFORM_LINKED_LIBS SnippetRender GL GLU ${GLUT_LIB} X11 rt pthread dl -Wl,-rpath='${ORIGIN}')
+ENDIF()
 
 IF(PX_GENERATE_GPU_STATIC_LIBRARIES)
 	LIST(APPEND SNIPPET_PLATFORM_LINKED_LIBS PhysXGpu)
diff --git a/physx/source/compiler/cmake/linux/CMakeLists.txt b/physx/source/compiler/cmake/linux/CMakeLists.txt
index 59c3d40..b4c865b 100644
--- a/physx/source/compiler/cmake/linux/CMakeLists.txt
+++ b/physx/source/compiler/cmake/linux/CMakeLists.txt
@@ -27,7 +27,13 @@
 STRING(TOLOWER "${CMAKE_BUILD_TYPE}" CMAKE_BUILD_TYPE_LOWERCASE)
 
 #TODO: Fix warnings
-SET(CLANG_WARNINGS "-ferror-limit=0 -Wall -Wextra -Werror -Weverything\
+IF(APPLE)
+	SET(CLANG_WARNINGS_AS_ERRORS "")
+ELSE()
+	SET(CLANG_WARNINGS_AS_ERRORS "-Werror")
+ENDIF()
+
+SET(CLANG_WARNINGS "-ferror-limit=0 -Wall -Wextra ${CLANG_WARNINGS_AS_ERRORS} -Weverything\
     -Wno-unused-but-set-variable \
     -Wno-switch-default \
     -Wno-cast-qual \
@@ -76,6 +82,10 @@ SET(CLANG_WARNINGS "-ferror-limit=0 -Wall -Wextra -Werror -Weverything\
     -Wno-zero-as-null-pointer-constant \
     -Wno-reserved-identifier \
     -Wno-undefined-func-template \
+    -Wno-allocator-wrappers \
+    -Wno-bitwise-instead-of-logical \
+    -Wno-missing-include-dirs \
+    -Wno-poison-system-directories \
 ")
 
 SET(GCC_WARNINGS "-Wall -Werror \
@@ -117,9 +127,15 @@ ELSE()
 	SET(AARCH64_FLAGS "")
 ENDIF()
 
-SET(COMMON_CXX_FLAGS "-std=c++14 -D_GLIBCXX_USE_CXX11_ABI=1 -fno-rtti -fno-exceptions -ffunction-sections -fdata-sections -fvisibility=hidden ${AARCH64_FLAGS}")
+IF(APPLE)
+	SET(STDLIB_ABI_FLAG "")
+ELSE()
+	SET(STDLIB_ABI_FLAG "-D_GLIBCXX_USE_CXX11_ABI=1")
+ENDIF()
+
+SET(COMMON_CXX_FLAGS "-std=c++14 ${STDLIB_ABI_FLAG} -fno-rtti -fno-exceptions -ffunction-sections -fdata-sections -fvisibility=hidden ${AARCH64_FLAGS}")
 
-IF ("${CMAKE_CXX_COMPILER_ID}" STREQUAL "Clang")
+IF ("${CMAKE_CXX_COMPILER_ID}" MATCHES "Clang")
 	IF ("${CMAKE_CXX_COMPILER_VERSION}" VERSION_LESS "10.0.0")
 		SET(PHYSX_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${COMMON_CXX_FLAGS} -fstrict-aliasing -Wstrict-aliasing=2 -Wno-shadow ${CLANG_WARNINGS}" CACHE INTERNAL "PhysX CXX")
 	ELSE()
diff --git a/physx/source/compiler/cmake/linux/PhysX.cmake b/physx/source/compiler/cmake/linux/PhysX.cmake
index 13add0e..21fc2fb 100644
--- a/physx/source/compiler/cmake/linux/PhysX.cmake
+++ b/physx/source/compiler/cmake/linux/PhysX.cmake
@@ -111,7 +111,11 @@ SET(PHYSX_PLATFORM_LINK_FLAGS_CHECKED " ")
 SET(PHYSX_PLATFORM_LINK_FLAGS_PROFILE " ")
 SET(PHYSX_PLATFORM_LINK_FLAGS_RELEASE " ")
 
-SET(PHYSX_PLATFORM_LINKED_LIBS dl)
+IF(APPLE)
+	SET(PHYSX_PLATFORM_LINKED_LIBS "")
+ELSE()
+	SET(PHYSX_PLATFORM_LINKED_LIBS dl)
+ENDIF()
 
 IF ("${CMAKE_CXX_COMPILER_ID}" STREQUAL "GNU")
   SET_SOURCE_FILES_PROPERTIES(${PX_SOURCE_DIR}/omnipvd/OmniPvdPxSampler.cpp PROPERTIES COMPILE_FLAGS "-fpermissive -Wno-error")
diff --git a/physx/source/compiler/cmake/linux/PhysXFoundation.cmake b/physx/source/compiler/cmake/linux/PhysXFoundation.cmake
index c0986a1..542bf03 100644
--- a/physx/source/compiler/cmake/linux/PhysXFoundation.cmake
+++ b/physx/source/compiler/cmake/linux/PhysXFoundation.cmake
@@ -32,7 +32,9 @@ IF(PX_GENERATE_STATIC_LIBRARIES)
 	SET(PHYSXFOUNDATION_LIBTYPE STATIC)	
 ELSE()
 	SET(PHYSXFOUNDATION_LIBTYPE SHARED)
-	SET(PHYSXFOUNDATION_PLATFORM_LINKED_LIBS rt)
+	IF(NOT APPLE)
+		SET(PHYSXFOUNDATION_PLATFORM_LINKED_LIBS rt)
+	ENDIF()
 	SET(PXFOUNDATION_LIBTYPE_DEFS
 		PX_PHYSX_FOUNDATION_EXPORTS;
 	)	
@@ -83,9 +85,13 @@ SET(PHYSXFOUNDATION_PLATFORM_FILES
 	${PHYSXFOUNDATION_RESOURCE_FILE}
 )
 
-SET(PHYSXFOUNDATION_PLATFORM_INCLUDES
-	${PHYSX_ROOT_DIR}/include/foundation/linux
-)
+IF(APPLE)
+	SET(PHYSXFOUNDATION_PLATFORM_INCLUDES "")
+ELSE()
+	SET(PHYSXFOUNDATION_PLATFORM_INCLUDES
+		${PHYSX_ROOT_DIR}/include/foundation/linux
+	)
+ENDIF()
 
 
 # Use generator expressions to set config specific preprocessor definitions
@@ -99,4 +105,8 @@ SET(PHYSXFOUNDATION_COMPILE_DEFS
 	$<$<CONFIG:release>:${PHYSX_LINUX_RELEASE_COMPILE_DEFS};>
 )
 
-SET(PXFOUNDATION_PLATFORM_LINK_FLAGS "-m64")
+IF(APPLE)
+	SET(PXFOUNDATION_PLATFORM_LINK_FLAGS "")
+ELSE()
+	SET(PXFOUNDATION_PLATFORM_LINK_FLAGS "-m64")
+ENDIF()
diff --git a/physx/source/foundation/FdTempAllocator.cpp b/physx/source/foundation/FdTempAllocator.cpp
index d90294b..481ae8f 100644
--- a/physx/source/foundation/FdTempAllocator.cpp
+++ b/physx/source/foundation/FdTempAllocator.cpp
@@ -64,7 +64,7 @@ void* PxTempAllocator::allocate(size_t size, const char* filename, PxI32 line)
 	if(!size)
 		return 0;
 
-	PxU32 index = PxMax(PxHighestSetBit(PxU32(size) + sizeof(Chunk) - 1), sMinIndex);
+	PxU32 index = PxMax(PxHighestSetBit(PxU32(PxU32(size) + sizeof(Chunk) - 1)), sMinIndex);
 
 	Chunk* chunk = 0;
 	if(index < sMaxIndex)
diff --git a/physx/source/foundation/unix/FdUnixFPU.cpp b/physx/source/foundation/unix/FdUnixFPU.cpp
index 22f3fb3..659068c 100644
--- a/physx/source/foundation/unix/FdUnixFPU.cpp
+++ b/physx/source/foundation/unix/FdUnixFPU.cpp
@@ -32,7 +32,7 @@
 PX_COMPILE_TIME_ASSERT(8 * sizeof(uint32_t) >= sizeof(fenv_t));
 #endif
 
-#if PX_OSX
+#if PX_OSX && PX_INTEL_FAMILY
 // osx defines SIMD as standard for floating point operations.
 #include <xmmintrin.h>
 #endif
@@ -41,7 +41,7 @@ physx::PxFPUGuard::PxFPUGuard()
 {
 #if defined(__CYGWIN__)
 #pragma message "FPUGuard::FPUGuard() is not implemented"
-#elif PX_OSX
+#elif PX_OSX && PX_INTEL_FAMILY
 	mControlWords[0] = _mm_getcsr();
 	// set default (disable exceptions: _MM_MASK_MASK) and FTZ (_MM_FLUSH_ZERO_ON), DAZ (_MM_DENORMALS_ZERO_ON: (1<<6))
 	_mm_setcsr(_MM_MASK_MASK | _MM_FLUSH_ZERO_ON | (1 << 6));
@@ -66,7 +66,7 @@ physx::PxFPUGuard::~PxFPUGuard()
 {
 #if defined(__CYGWIN__)
 #pragma message "PxFPUGuard::~PxFPUGuard() is not implemented"
-#elif PX_OSX
+#elif PX_OSX && PX_INTEL_FAMILY
 	// restore control word and clear exception flags
 	// (setting exception state flags cause exceptions on the first following fp operation)
 	_mm_setcsr(mControlWords[0] & ~_MM_EXCEPT_MASK);
@@ -82,7 +82,7 @@ PX_FOUNDATION_API void physx::PxEnableFPExceptions()
 #if PX_LINUX && !defined(__EMSCRIPTEN__)
 	feclearexcept(FE_ALL_EXCEPT);
 	feenableexcept(FE_INVALID | FE_DIVBYZERO | FE_OVERFLOW);
-#elif PX_OSX
+#elif PX_OSX && PX_INTEL_FAMILY
 	// clear any pending exceptions
 	// (setting exception state flags cause exceptions on the first following fp operation)
 	uint32_t control = _mm_getcsr() & ~_MM_EXCEPT_MASK;
@@ -98,7 +98,7 @@ PX_FOUNDATION_API void physx::PxDisableFPExceptions()
 {
 #if PX_LINUX && !defined(__EMSCRIPTEN__)
 	fedisableexcept(FE_ALL_EXCEPT);
-#elif PX_OSX
+#elif PX_OSX && PX_INTEL_FAMILY
 	// clear any pending exceptions
 	// (setting exception state flags cause exceptions on the first following fp operation)
 	uint32_t control = _mm_getcsr() & ~_MM_EXCEPT_MASK;
diff --git a/physx/source/physxextensions/src/serialization/SnSerialUtils.cpp b/physx/source/physxextensions/src/serialization/SnSerialUtils.cpp
index d79f6f6..8df5801 100644
--- a/physx/source/physxextensions/src/serialization/SnSerialUtils.cpp
+++ b/physx/source/physxextensions/src/serialization/SnSerialUtils.cpp
@@ -38,7 +38,7 @@ using namespace physx;
 namespace
 {
 
-#define SN_NUM_BINARY_PLATFORMS 9
+#define SN_NUM_BINARY_PLATFORMS 10
 const PxU32 sBinaryPlatformTags[SN_NUM_BINARY_PLATFORMS] =
 {
 	PX_MAKE_FOURCC('W','_','3','2'),
@@ -49,7 +49,8 @@ const PxU32 sBinaryPlatformTags[SN_NUM_BINARY_PLATFORMS] =
 	PX_MAKE_FOURCC('M','_','6','4'),
 	PX_MAKE_FOURCC('N','X','3','2'),
 	PX_MAKE_FOURCC('N','X','6','4'),
-	PX_MAKE_FOURCC('L','A','6','4')
+	PX_MAKE_FOURCC('L','A','6','4'),
+	PX_MAKE_FOURCC('M','A','6','4')
 };
 
 const char* sBinaryPlatformNames[SN_NUM_BINARY_PLATFORMS] =
@@ -62,7 +63,8 @@ const char* sBinaryPlatformNames[SN_NUM_BINARY_PLATFORMS] =
 	"mac64",
 	"switch32",
 	"switch64",
-	"linuxaarch64"
+	"linuxaarch64",
+	"macaarch64"
 };
 
 }
@@ -89,6 +91,8 @@ PxU32 getBinaryPlatformTag()
 	return sBinaryPlatformTags[7];
 #elif PX_LINUX && PX_A64
 	return sBinaryPlatformTags[8];
+#elif PX_OSX && PX_A64
+	return sBinaryPlatformTags[9];
 #else
 	#error Unknown binary platform
 #endif
